# Required metadata
sonar.projectKey=DATE_LAST_NEXT_CONTACT
sonar.projectName=DATE_LAST_NEXT_CONTACT
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=main
sonar.exclusions=main/not_upgrade_safe

# Language
sonar.language=php

# Encoding of the source files
sonar.sourceEncoding=UTF-8